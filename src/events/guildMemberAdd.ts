import { Events } from 'discord.js'; import { guildConfig } from '../models/GuildConfig.js'; import { log } from '../utils/logger.js'; export default {name:Events.GuildMemberAdd,async execute(_c:any,m:any){const cfg=await guildConfig(m.guild.id);if(cfg.raidMode){if(m.user.bot||m.member?.moderatable)await m.kick('Raid mode actif').catch(()=>null);await log(m.client,m.guild,'AntiRaid',`${m.user.tag} bloqué pendant le raid mode.`);}}};

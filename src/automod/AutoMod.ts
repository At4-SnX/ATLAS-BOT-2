import { Message } from 'discord.js'; import { env } from '../config/env.js'; import { Warn } from '../models/Warn.js'; import { log } from '../utils/logger.js';
const normalize=(s:string)=>s.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9\s]/g,'');
export async function runAutoMod(message: Message){if(!message.guild||message.author.bot||message.member?.permissions.has('ManageMessages'))return;const raw=message.content.toLowerCase(),clean=normalize(raw);const forbidden=[...env.badWords,...env.badWordsShort,...env.phishingWords];const hit=forbidden.find(w=>clean.includes(normalize(w)))||(/discord(?:\.gg|\.com\/invite)\//i.test(raw)?'invitation Discord':null)||(/https?:\/\/[^\s]+/i.test(raw)&&/(nitro|steam|gift|login|verify)/i.test(raw)?'lien suspect':null);if(!hit)return;await message.delete().catch(()=>null);const warnId=`A-${Date.now().toString(36).toUpperCase()}`;await Warn.create({guildId:message.guild.id,userId:message.author.id,moderatorId:message.client.user!.id,reason:`AutoMod: ${hit}`,warnId});if(env.automodAction==='timeout'&&message.member?.moderatable)await message.member.timeout(env.automodTimeoutMinutes*60_000,`AutoMod: ${hit}`).catch(()=>null);await log(message.client,message.guild,'AutoMod',`${message.author.tag}: ${hit}`);}
